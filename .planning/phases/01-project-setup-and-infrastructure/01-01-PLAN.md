---
phase: 01-project-setup-and-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - surt/config.py
  - surt/__init__.py
  - requirements.txt
  - setup_pod.sh
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "requirements.txt contains all 7 Python dependencies (transformers, datasets, audiomentations, jiwer, huggingface_hub, accelerate, evaluate)"
    - "setup_pod.sh installs system deps (tmux, libsndfile1), pip deps, verifies HF_TOKEN, verifies GPU, creates workspace dirs, and starts tmux session"
    - "config.py detects GPU name via torch.cuda and maps it to batch_size and gradient_accumulation_steps targeting effective batch 32"
    - "config.py has conservative fallback (batch=4, accum=8) for unknown GPUs"
    - "config.py exposes all training constants (EVAL_STEPS, SAVE_STEPS, WARMUP_STEPS, GENERATION_MAX_LENGTH, MOOL_MANTAR, etc.)"
  artifacts:
    - path: "surt/config.py"
      provides: "GPU auto-detection, batch sizing, training constants, paths"
      contains: "torch.cuda.get_device_name"
    - path: "surt/__init__.py"
      provides: "Python package marker"
    - path: "requirements.txt"
      provides: "All pip dependencies for single-command install"
      contains: "audiomentations"
    - path: "setup_pod.sh"
      provides: "One-shot RunPod pod provisioning"
      contains: "apt-get install -y tmux libsndfile1"
  key_links:
    - from: "setup_pod.sh"
      to: "requirements.txt"
      via: "pip install -r requirements.txt"
      pattern: "pip install -r requirements"
    - from: "setup_pod.sh"
      to: "surt/config.py"
      via: "python -c import to validate GPU detection"
      pattern: "python.*config"
    - from: "surt/config.py"
      to: "torch.cuda"
      via: "GPU name detection at import time"
      pattern: "torch\\.cuda\\.get_device_name"
---

<objective>
Create the complete project scaffolding for the Surt training pipeline: the `surt/` Python package with GPU auto-detection config module, a `requirements.txt` for single-command dependency install, and a `setup_pod.sh` script that provisions a fresh RunPod pod end-to-end.

Purpose: Establish the foundation that every subsequent phase imports from (config.py) and that makes pod provisioning reproducible and idempotent.
Output: 4 files (surt/__init__.py, surt/config.py, requirements.txt, setup_pod.sh) ready to deploy on RunPod.
</objective>

<execution_context>
@/Users/surindersingh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/surindersingh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-and-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create surt/ package with config.py GPU auto-detection module</name>
  <files>surt/__init__.py, surt/config.py</files>
  <action>
Create the `surt/` directory as a Python package.

**surt/__init__.py**: Empty file (package marker only).

**surt/config.py**: GPU auto-detection config module with the following structure:

1. **GPU Detection Block** (top of file):
   - `GPU_NAME = torch.cuda.get_device_name(0) if torch.cuda.is_available() else "cpu"`
   - If/elif chain mapping GPU names to `BATCH_SIZE` and `GRAD_ACCUM`:
     - A100, A40: batch=16, accum=2
     - 4090, 3090, A5000, L4: batch=8, accum=4
     - T4, V100: batch=4, accum=8
     - else (fallback): batch=4, accum=8
   - `EFFECTIVE_BATCH = BATCH_SIZE * GRAD_ACCUM` (always 32)
   - Print statement: `[config] GPU: {GPU_NAME}` and `[config] Batch: {BATCH_SIZE} x Accum: {GRAD_ACCUM} = Effective: {EFFECTIVE_BATCH}`

2. **Paths Block**:
   - `OUTPUT_DIR = "/workspace/surt/checkpoints"`
   - `LOG_DIR = "/workspace/surt/logs"`

3. **Model Constants Block**:
   - `BASE_MODEL = "openai/whisper-small"`
   - `HF_MODEL_REPO = "surindersingh/surt-small-v1"` with comment "user sets this"

4. **Training Constants Block**:
   - `EVAL_STEPS = 300`
   - `SAVE_STEPS = 300`
   - `SAVE_TOTAL_LIMIT = 3`
   - `WARMUP_STEPS = 400`
   - `GENERATION_MAX_LENGTH = 448` with comment explaining Gurmukhi tokenizer expansion (3-5x longer than English)

5. **Gurmukhi Anchor**:
   - `MOOL_MANTAR = "ੴ ਸਤਿ ਨਾਮੁ ਕਰਤਾ ਪੁਰਖੁ ਨਿਰਭਉ ਨਿਰਵੈਰੁ ਅਕਾਲ ਮੂਰਤਿ ਅਜੂਨੀ ਸੈਭੰ ਗੁਰ ਪ੍ਰਸਾਦਿ"`

IMPORTANT: Do NOT `pip install torch` or any dependencies. This file must be syntactically valid Python but will only fully execute on a GPU machine. Use `torch` import at the top level (it will be available on RunPod).

Follow the exact pattern from 01-RESEARCH.md "Pattern 1: GPU Auto-Detection Config Module".
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('surt/config.py').read()); print('syntax ok')" && test -f surt/__init__.py && echo "init exists"</automated>
  </verify>
  <done>surt/__init__.py exists as empty package marker. surt/config.py is syntactically valid Python, contains GPU detection via torch.cuda.get_device_name, maps 8+ GPU types to batch/accum settings with conservative fallback, exposes all training constants (EVAL_STEPS=300, SAVE_STEPS=300, WARMUP_STEPS=400, GENERATION_MAX_LENGTH=448, MOOL_MANTAR), and prints config on import.</done>
</task>

<task type="auto">
  <name>Task 2: Create requirements.txt and setup_pod.sh provisioning script</name>
  <files>requirements.txt, setup_pod.sh</files>
  <action>
**requirements.txt**: List all Python dependencies, one per line, with minimum version pins:
```
transformers>=4.46
datasets>=2.16
audiomentations>=0.42
jiwer>=4.0
huggingface_hub>=0.20
accelerate>=0.26
evaluate>=0.4
```

Do NOT include torch/torchaudio (provided by RunPod template). Do NOT include pyroomacoustics (pulled in by audiomentations if needed -- validated during setup).

**setup_pod.sh**: Bash script with `#!/bin/bash` and `set -euo pipefail`. Must be idempotent (safe to re-run). Structure:

1. **System dependencies**: `apt-get update && apt-get install -y tmux libsndfile1`
2. **Python dependencies**: `pip install -r requirements.txt`
3. **Verify Python imports**: `python -c "import transformers, datasets, audiomentations, jiwer, huggingface_hub; print('All imports OK')"`
4. **Verify HuggingFace auth**: Check `HF_TOKEN` env var is set (exit 1 with helpful message if not). Then run `python -c "from huggingface_hub import HfApi; user = HfApi().whoami(); print(f'Logged in as: {user[\"name\"]}')"`.
5. **Verify GPU**: `python -c "import torch; print(f'GPU: {torch.cuda.get_device_name(0)}'); print(f'VRAM: {torch.cuda.get_device_properties(0).total_mem / 1e9:.1f} GB')"`
6. **Create workspace directories**: `mkdir -p /workspace/surt/checkpoints /workspace/surt/logs`
7. **Verify config module**: `python -c "from surt.config import GPU_NAME, BATCH_SIZE, GRAD_ACCUM, EFFECTIVE_BATCH; print(f'Config OK: {GPU_NAME} -> batch={BATCH_SIZE} accum={GRAD_ACCUM} eff={EFFECTIVE_BATCH}')"`
8. **Start tmux session**: `tmux new-session -d -s surt || echo "tmux session 'surt' already exists"` (idempotent)
9. **Print summary**: Echo setup complete message with instruction to attach via `tmux attach -t surt`

Each section should print a header (e.g., `echo "=== Installing system dependencies ==="`) for clear output.

Make setup_pod.sh executable: `chmod +x setup_pod.sh`.

Follow the exact pattern from 01-RESEARCH.md "Pattern 2: One-Shot Pod Setup Script" but add step 7 (config module verification) which is not in the research pattern.
  </action>
  <verify>
    <automated>bash -n setup_pod.sh && echo "shell syntax ok" && test -f requirements.txt && python -c "lines = open('requirements.txt').readlines(); assert len([l for l in lines if l.strip() and not l.startswith('#')]) >= 7, f'Expected 7+ deps, got {len(lines)}'; print(f'{len([l for l in lines if l.strip() and not l.startswith(\"#\")])} dependencies listed')"</automated>
  </verify>
  <done>requirements.txt lists 7 Python dependencies with version pins (no torch). setup_pod.sh is executable, passes bash syntax check, installs system deps (tmux, libsndfile1), pip deps from requirements.txt, verifies HF_TOKEN is set, verifies GPU, verifies config.py imports, creates workspace dirs, starts tmux session, and is idempotent.</done>
</task>

</tasks>

<verification>
1. `python -c "import ast; ast.parse(open('surt/config.py').read())"` -- config.py is valid Python
2. `bash -n setup_pod.sh` -- setup script has valid shell syntax
3. `test -x setup_pod.sh` -- setup script is executable
4. `grep -c "torch.cuda.get_device_name" surt/config.py` -- GPU detection is present
5. `grep -c "MOOL_MANTAR" surt/config.py` -- Gurmukhi anchor is present
6. `grep -c "audiomentations" requirements.txt` -- all deps present
7. `grep -c "tmux" setup_pod.sh` -- tmux installation and session management present
8. `grep -c "HF_TOKEN" setup_pod.sh` -- HF token verification present
</verification>

<success_criteria>
- 4 files created: surt/__init__.py, surt/config.py, requirements.txt, setup_pod.sh
- config.py has GPU auto-detection with 8+ GPU types and conservative fallback
- config.py exposes all training constants needed by subsequent phases
- requirements.txt has 7 deps with version pins, no torch
- setup_pod.sh is executable, idempotent, and validates entire environment
- All files pass syntax validation
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-and-infrastructure/01-01-SUMMARY.md`
</output>
